# Bicep build output
**/*.bicep.json
automation/bicep/main.json

# Terraform
**/.terraform/
**/.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# OS / editor
.DS_Store
Thumbs.db

# Local-only perf-comparison script (kept out of the repo)
scripts/Set-LawTableRetention.Sequential.ps1

# Draft Tech Community article (kept out of the repo)
article-techcommunity.md
